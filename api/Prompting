I need to move on from the Streamlit application to the FAST API and React app



    (Main Idea)
        Okay. We need to explain the situation in a very good way. So what I can take from
         your side some good suggestions here. I have been doing a project. I had in mind
        the idea that the UI will be on Streamlit. However, The supervisor has the plans changed.
        This means that I want to use FASTAPI and React to make the UI. Well great news I have no
        idea about both of these and this is a very great challenge or learning both things and
        delivering something as well. So I will give you the link. And give me an idea on the
        code structure that will happen if we use the FAST API and the React .What will actually
        change here.

    (Steps to Move Through)
        These are my guesses to what are the first step to move in
        1. Install the needed things (pip install --- )
        2. Design a UI until is it completely done. Because this would help us understand what are
            the api function we need to implement. (We have search bars and many stuff. Which means
             there are lots of new functions in the api that I need to create).
        3. Estimate the functions we need in the API layer after that. Write a prompt for each one
            including the functionality and the signature of each one (And how to use the UI Object)
            This UI object is also not design yet.







    (Current Code Structure)

        This means I have to ask myself the right questions here.
        What are the end points that I have and need to isolate here

        The db_layer:
            1. Has set of functions that deal directly with the database
            2. We mean by that get , fetch , edit and delete functions

        The API Layer
            1. Has the Add function that adds a new record
            2. Has extract function, that runs NER Model as well as the Classification Model
                (From the model_directory folder)
            3. Has the generate report button. That also uses bd_layer function to retrieve data
                and organize the data in a very good way.

        The model Directory
            1. A very large folder that has a function that packages all the models inside
            2. Has STT models and NER models and One Classification model to be called (it does 8 Classifications)


        (Current Status)
                I am waiting some people to give me the schema of the new database. The project
            is for them so I need to wait so that they give me all the features to design the schema.
            For that I prefer starting with UI and api prompting as I don't have a database schema yet.

        (Look on the Code)
            [db_layer empty code]:

                #This is going to deal with Microsoft SQL Server
            #So we have a database that consists of 2 or 3 tables at most
            #We should be able to add record into the database and retreive records from the database
            #And edit them as well


            def fetch_records():
                pass


            def add_record():
                pass


            def edit_record():
                pass

            def delete_record():
                pass

            def get_record():
                pass


        [api_layer empty code]:

            #Here are the functions that deal with the UI interactions
            #We have the extract that does NER and Classifications
            #We have generate





                def extract():
                    pass


                def add():
                    pass

                # As not all information in the database is needed to be presented in the website we need to create
                # a UI object. That is ab object that is easily presented in the UI. It is not the same record d
                # data structure that we deal with in the backend
                def get():
                    #Takes conditions from the UI and uses the backend functions
                    pass

                def filter():
                    pass

                def generate_report():
                    pass

                def get_incident():
                    pass


