You are helping me design backend API contracts and data requirements for a React frontend page.

Context:
- This is Iteration 1 only
- The UI is considered finalized
- Sensitive hospital data
- Offline server environment
- This page is an ADMIN / CONFIGURATION page
- Editing and write operations are explicitly allowed here
- No future features, no speculation

Page Name:
SettingPage

Description:
This page is the system configuration and administration center.
It is used by authorized users to manage:

1. Organizational structure:
   - Departments (hierarchical)
   - Internal vs external mapping modes
2. Human resources:
   - Doctors and their department associations
3. System configuration:
   - Variable attributes (lookup values used across the system)
   - Policy and threshold configurations used by reporting and investigation pages

This page defines reference data that is consumed by:
- InsertRecord
- EditRecord
- ReportingPage
- InvestigationPage
- TableView
- TrendMonitoringPage

Changes here affect the entire system.

UI Components:
- Department mapping mode toggle (internal / external)
- Global settings action bar:
  - Save configuration
  - Refresh data
  - Export configuration (JSON / CSV)
- Tabbed interface with 4 sections:
  1. Departments:
     - Add department form
     - Department table (edit / delete)
     - Hierarchical parent-child structure
  2. Doctors:
     - Add doctor form
     - Doctor table (edit / delete)
     - Department assignment
  3. Variable Attributes:
     - Manage system-wide enumerations (severity, stages, domains, etc.)
  4. Policy Configuration:
     - Thresholds
     - Policy values used in reporting and investigation logic

Your Tasks:

1. Extract **Data Needs** for this page:
   - List all data fields required for:
     - Departments (hierarchy, type, mapping mode)
     - Doctors (identity, department linkage)
     - Variable attributes (key-value / id-label pairs)
     - Policy configuration values (thresholds, flags, limits)
   - Group fields by logical object:
     - Department
     - Doctor
     - VariableAttribute
     - PolicyConfiguration
   - Mark:
     - Reference data used by other pages
     - Configuration values that influence analytics or reports

2. Propose **API Endpoints** for this page:
   - Read endpoints:
     - Fetch departments (by mapping mode)
     - Fetch doctors
     - Fetch variable attributes
     - Fetch policy configuration
   - Write endpoints:
     - Add / update / delete department
     - Add / update / delete doctor
     - Save variable attributes
     - Save policy configuration
   - Configuration endpoints:
     - Save full configuration snapshot
     - Export configuration (JSON / CSV)
   - For each endpoint specify:
     - Endpoint path
     - HTTP method
     - Input parameters
     - Output shape (simple JSON description)

3. Identify **assumptions and open questions**, such as:
   - Who is authorized to access this page
   - Whether changes require audit logging
   - Whether configuration changes are versioned
   - Whether configuration changes take effect immediately or require reload
   - Whether external mapping mode affects reporting logic
   - Whether policy configuration is global or per-department

Constraints:
- Do NOT design database tables
- Do NOT write SQL or backend code
- Do NOT redesign UI behavior
- Keep naming consistent with InsertRecord, ReportingPage, and InvestigationPage
- Avoid overengineering

Output Format:
Section A: Data Needs (tables)
Section B: API Endpoints (tables)
Section C: Open Questions / Risks
