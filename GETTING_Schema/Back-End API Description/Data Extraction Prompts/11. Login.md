You are helping me design backend API contracts and data requirements for a React frontend page.

Context:
- This is Iteration 1 only
- The UI is considered finalized
- No future features, no speculation
- Sensitive hospital data
- Server is offline (no external auth providers)
- Authentication is required before accessing any other page

Page Name:
Login

Description:
This page allows a user to authenticate into the system.
If a valid authentication token already exists locally, the user is redirected
to the main application without seeing the login form.

The page consists of:
- A system logo
- A login form (credentials input)
- A footer note (no data dependency)

UI Components:
- Login form:
  - User identifier input (e.g. username / employee ID / email)
  - Password input
  - Submit button
- Automatic redirect if already authenticated
- No registration, no password reset, no MFA in Iteration 1

Your Tasks:

1. Extract **Data Needs** for this page:
   - Identify all fields required to submit a login request
   - Identify all data returned on successful authentication
   - Identify any locally stored data required for session persistence
   - Group data needs by logical object (User, Session, Role/Permission)
   - Clearly distinguish:
     - Input credentials
     - Authentication result
     - Authorization context (if any)

2. Propose **API Endpoints** for this page:
   - Design minimal authentication endpoints suitable for an offline system
   - For each endpoint, specify:
     - Endpoint path
     - HTTP method
     - Input parameters
     - Output shape (token, user identity, permissions)
   - Include:
     - Login endpoint
     - Optional token validation / session check endpoint
   - Assume frontend stores token securely (e.g. localStorage) for Iteration 1

3. Identify **assumptions and open questions**, such as:
   - What identifier is used to log in (username, employee ID, etc.)
   - Whether roles/permissions are needed in Iteration 1
   - Token type (opaque vs JWT-like)
   - Token expiration and refresh strategy
   - Logout handling (client-side vs server-side)

Constraints:
- Do NOT design database tables
- Do NOT write SQL or Python code
- Do NOT introduce external authentication providers
- Do NOT design user management flows (create/edit users)
- Keep authentication simple and internal
- Avoid overengineering

Output Format:
Section A: Data Needs (tables)
Section B: API Endpoints (tables)
Section C: Open Questions / Risks
