You are helping me design backend API contracts and data requirements for a React frontend page.

Context:
- This is Iteration 1 only
- The UI is considered finalized
- Sensitive hospital data
- Offline server environment
- This is the MAIN operational data table of the system
- Focus primarily on READ operations
- Limited write/navigation actions may exist (view, edit, export)
- No future features, no speculation

Page Name:
TableView

Description:
This page is the central operational view of all feedback / incident records.
It is used by staff to:

- Search records by ID or patient name
- Filter records by:
  - Issuing department
  - Target department
  - Source
- Switch between predefined table “views” (e.g. complete view vs simplified views)
- Inspect record status (open, in progress, closed, red flag, etc.)
- Navigate to record details or edit pages
- Export filtered results

This page acts as:
- The primary entry point to EditRecord
- A feeder for Export, FollowUp, Investigation, and Reporting workflows

UI Components:
- Page header and description
- View selector:
  - Allows switching between different column presets (e.g. complete view)
- Search and filter bar:
  - Free-text search (record ID, patient name)
  - Issuing department filter
  - Target department filter
  - Source filter
- Records table:
  - Displays one row per record
  - Shows record metadata, classification, severity, status, and flags
- Export section:
  - Shows number of filtered records
  - Allows exporting filtered results

Your Tasks:

1. Extract **Data Needs** for this page:
   - List all data fields required to render a single table row
   - Group fields by logical object:
     - Record / Incident
     - Patient
     - Departments (issuing / target)
     - Classification (domain, category, subcategory)
     - Severity, stage, harm
     - Status and flags (red flag, improvement opportunity)
   - Clearly mark:
     - Derived or computed fields (labels, flags, display status)
     - Fields used only for filtering
     - Fields used only for display
   - Distinguish between:
     - Raw IDs
     - Human-readable labels

2. Propose **API Endpoints** for this page:
   - Primary list endpoint:
     - Fetch paginated records
     - Supports filtering and search
   - Optional endpoints:
     - Fetch available filter options (departments, sources, statuses)
     - Export records (CSV / JSON)
   - For each endpoint specify:
     - Endpoint path
     - HTTP method
     - Input parameters (search, filters, pagination)
     - Output shape (simple JSON structure)

3. Identify **assumptions and open questions**, such as:
   - Pagination vs full dataset loading
   - Server-side vs client-side filtering
   - Maximum record count expected
   - Whether export uses same filters as table
   - Whether user role affects visible columns or records
   - Whether “views” are frontend-only or server-defined

Constraints:
- Do NOT design database tables
- Do NOT write SQL or backend code
- Do NOT redesign table columns or filters
- Keep naming consistent with:
  - InsertRecord
  - EditRecord
  - ReportingPage
  - InvestigationPage
- Avoid overengineering

Output Format:
Section A: Data Needs (tables)
Section B: API Endpoints (tables)
Section C: Open Questions / Risks
