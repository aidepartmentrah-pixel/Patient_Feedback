You are helping me design backend API contracts and data requirements for a React frontend page.

Context:
- This is Iteration 1 only
- The UI is considered finalized
- No future features, no speculation
- Sensitive hospital data, offline server
- This page is READ-ONLY and exploratory
- Focus on aggregated and grouped data, not raw records

Page Name:
Investigation Page (Root Cause Investigation)

Description:
This page is an exploratory analysis tool used to investigate why certain parts
of the organization crossed incident thresholds during a specific time period.

The user selects:
- A time period (season / quarter)
- An organizational scope (administration → department → section)
- A visualization type (tree-based aggregations)

The system then renders a hierarchical tree visualization that shows
incident concentration and distribution based on the selected tree type.

UI Components:
- Filters / selectors:
  - Season / reporting period
  - Administration (optional)
  - Department (optional, depends on administration)
  - Section (optional, depends on department)
- Visualization type selector:
  - Incident count
  - Domain distribution (numbers)
  - Domain distribution (percentage)
  - Severity distribution (numbers)
  - Severity distribution (percentage)
  - Red flag incidents
  - Never ever incidents
- Tree visualization:
  - Hierarchical aggregation across organization levels
  - Each node displays a numeric value and visual emphasis
- Informational footer (no data dependency)

Your Tasks:

1. Extract **Data Needs** for this page:
   - Identify all input parameters required to perform an investigation query
   - Identify the aggregated metrics needed to build the investigation tree
   - Group data needs by logical concept:
     - Time period / season
     - Organizational hierarchy (administration, department, section)
     - Incident aggregation metrics
   - Clearly distinguish:
     - Raw filters vs derived values
     - Absolute counts vs percentages
   - Do NOT include raw incident-level fields unless strictly required for aggregation

2. Propose **API Endpoints** for this page:
   - Design read-only endpoints that return aggregated data suitable for tree rendering
   - For each endpoint, specify:
     - Endpoint path
     - HTTP method
     - Input parameters (season, scope, tree type)
     - Output shape describing:
       - Tree nodes
       - Hierarchy levels
       - Metric values per node
   - Assume the frontend handles rendering logic
   - One flexible endpoint is preferred over many specialized ones

3. Identify **assumptions and open questions**, such as:
   - How seasons / periods are defined (calendar vs custom)
   - Whether percentages are calculated backend or frontend
   - How red flag / never ever incidents are identified
   - Maximum hierarchy depth expected
   - Performance considerations for large datasets

Constraints:
- Do NOT design database tables
- Do NOT write SQL or Python code
- Do NOT introduce AI/ML logic
- Do NOT invent workflows beyond what the UI shows
- Keep naming neutral, analytical, and consistent
- Avoid overengineering

Output Format:
Section A: Data Needs (tables)
Section B: API Endpoints (tables)
Section C: Open Questions / Risks
