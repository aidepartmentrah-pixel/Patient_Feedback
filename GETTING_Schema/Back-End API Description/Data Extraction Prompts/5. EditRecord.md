You are helping me design backend API contracts and data requirements for a React frontend page.

Context:
- This is Iteration 1 only
- The UI is considered finalized
- No future features, no speculation
- Sensitive hospital data, offline server
- This page supports BOTH read and update operations on a single record
- Focus on correctness, traceability, and minimal required writes

Page Name:
Edit Record

Description:
This page allows users to search for, select, view, and update an existing feedback / incident record.
The workflow is:
1. Search and select a record
2. Load the full record details
3. Edit structured fields and text fields
4. Update the record
5. Optionally reset changes or cancel editing

This page edits ONE record at a time.

UI Components:
- Record search component (search + select existing record)
- Text blocks:
  - Complaint text
  - Immediate action
  - Taken action
- Metadata form fields:
  - Dates
  - Issuing department
  - Target department
  - Source
  - Status
  - Severity
  - Stage
  - Harm level
  - Improvement type
- Entity fields:
  - Patient name
  - Doctor name
  - Other detected entities
- Classification fields:
  - Category
  - Sub-category
  - Classification (Arabic)
- NER output section (editable fields)
- Action buttons:
  - Update record
  - Reset to original
  - Cancel editing

Your Tasks:

1. Extract **Data Needs** for this page:
   - Identify data required to:
     - Search for records
     - Load a single record by ID
   - Identify all editable fields grouped by logical object:
     - Incident / Record core data
     - Patient-related fields (as displayed)
     - Doctor-related fields (as displayed)
     - Classification and taxonomy fields
     - NLP / NER-derived fields
   - Identify non-editable or derived fields (e.g. record ID, original values)
   - Clearly mark:
     - Fields required for update
     - Fields required only for display

2. Propose **API Endpoints** for this page:
   - Endpoints for:
     - Searching records (lightweight list)
     - Fetching a single record by ID
     - Updating an existing record
   - For each endpoint, specify:
     - Endpoint path
     - HTTP method (GET / PUT or PATCH)
     - Input parameters (record_id, payload fields)
     - Output shape (updated record or status)
   - Consider validation and partial updates (only changed fields)

3. Identify **assumptions and open questions**, such as:
   - Who is allowed to edit records
   - Whether edits are audited / versioned
   - Whether some fields are immutable after creation
   - How concurrent edits are handled
   - Whether NER outputs are recalculated or stored as-is

Constraints:
- Do NOT design database tables
- Do NOT write SQL or Python code
- Do NOT invent workflows not visible in the UI
- Keep endpoint naming clear and consistent
- Avoid overengineering (no approval workflows)

Output Format:
Section A: Data Needs (tables)
Section B: API Endpoints (tables)
Section C: Assumptions / Open Questions / Risks
