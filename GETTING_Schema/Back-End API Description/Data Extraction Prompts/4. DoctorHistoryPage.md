You are helping me design backend API contracts and data requirements for a React frontend page.

Context:
- This is Iteration 1 only
- The UI is considered finalized
- No future features, no speculation
- Focus on READ operations only
- Sensitive hospital data, offline server
- This page is analytics and history focused (no data editing)

Page Name:
Doctor History & Performance Analysis

Description:
This page allows users to search for a doctor and view:
- Doctor profile information
- Incident-related statistics
- Performance analytics
- Historical incident records linked to that doctor

The page is read-only and used for analysis, review, and reporting.

UI Components:
- Doctor search component (search + select doctor)
- Doctor profile card (basic doctor metadata)
- Statistic summary cards (incident counts by severity, red flags)
- Charts:
  - Incident category breakdown
  - Monthly incident trend
- Incidents table (list of incidents linked to the doctor)
- Report actions (e.g. export / generate report)

Your Tasks:

1. Extract **Data Needs** for this page:
   - Identify data required to search and select doctors
   - Identify doctor profile fields displayed
   - Identify data required for doctor-level statistics:
     - Total incidents
     - Severity breakdown
     - Red flag counts
   - Identify data required for analytics:
     - Category distribution
     - Monthly trend
   - Identify data required for the incidents table:
     - Incident identifiers
     - Date
     - Patient reference (as displayed)
     - Category
     - Severity
     - Status
     - Red flag indicator
   - Clearly mark:
     - Aggregated / derived fields (counts, trends)
     - Raw fields

2. Propose **API Endpoints** for this page:
   - Endpoints for:
     - Searching / listing doctors
     - Fetching a doctor profile
     - Fetching doctor-level statistics
     - Fetching doctor analytics (charts data)
     - Fetching incidents linked to a doctor
   - For each endpoint, specify:
     - Endpoint path
     - HTTP method (likely GET)
     - Input parameters (doctor_id, date range if applicable)
     - Output shape (high-level description or simple JSON)

3. Identify **assumptions and open questions**, such as:
   - How doctors are uniquely identified (internal ID vs employee ID)
   - Whether inactive doctors are included
   - Default date range for analytics
   - Whether patient identifiers should be masked or limited

Constraints:
- Do NOT design database tables
- Do NOT write SQL or Python code
- Do NOT introduce write operations
- Keep naming clear and consistent
- Avoid overengineering

Output Format:
Section A: Data Needs (tables)
Section B: API Endpoints (tables)
Section C: Assumptions / Open Questions / Risks
