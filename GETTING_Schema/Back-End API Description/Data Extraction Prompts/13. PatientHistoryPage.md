You are helping me design backend API contracts and data requirements for a React frontend page.

Context:
- This is Iteration 1 only
- The UI is considered finalized
- No future features, no speculation
- Sensitive hospital and patient data
- Offline server environment
- This page is READ-ONLY (no edits, no analytics, no predictions)

Page Name:
PatientHistoryPage

Description:
This page allows authorized users to search for a patient and view their
historical feedback / incident records.

The page focuses on factual patient history only:
- Basic patient profile information
- A chronological table of related feedback / incidents
- Simple refresh and export actions
- No charts, no scoring, no AI analysis

UI Components:
- Patient search component
- Patient information card:
  - Demographic and contact information
  - Total number of incidents (derived)
- Action buttons:
  - Refresh patient data
  - Export patient history (CSV / JSON)
- Feedback table:
  - Date
  - Department
  - Category
  - Severity
  - Doctor name
  - Status
  - Description

Your Tasks:

1. Extract **Data Needs** for this page:
   - List all data fields required to:
     - Search for a patient
     - Display patient profile information
     - Populate the feedback / incident table
     - Support export functionality
   - Group data by logical object:
     - Patient
     - Incident / Feedback
     - Department
     - Doctor
   - Mark:
     - Derived fields (e.g., total incident count)
     - Optional fields (e.g., description)

2. Propose **API Endpoints** for this page:
   - Design read-only endpoints to:
     - Search patients
     - Fetch a single patient profile
     - Fetch all feedback / incidents related to a patient
     - Export patient history data
   - For each endpoint specify:
     - Endpoint path
     - HTTP method
     - Input parameters (patient ID, search query)
     - Output shape (descriptive JSON)
   - Assume:
     - Pagination may be needed for large histories
     - Export endpoints may reuse existing read endpoints

3. Identify **assumptions and open questions**, such as:
   - How patients are uniquely identified (ID, MRN, etc.)
   - Whether anonymization or masking is required
   - Access control rules for patient data
   - Maximum history depth to return by default
   - Whether export actions should be logged or audited

Constraints:
- Do NOT design database tables
- Do NOT write SQL or Python code
- Do NOT introduce analytics or AI logic
- Do NOT add write/update/delete operations
- Keep naming neutral, medical, and privacy-aware
- Avoid overengineering

Output Format:
Section A: Data Needs (tables)
Section B: API Endpoints (tables)
Section C: Open Questions / Risks
