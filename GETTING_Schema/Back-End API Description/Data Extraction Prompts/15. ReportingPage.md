You are helping me design backend API contracts and data requirements for a React frontend page.

Context:
- This is Iteration 1 only
- The UI is considered finalized
- No future features, no speculation
- Sensitive hospital data
- Offline server environment
- Focus on READ operations
- Export actions (PDF / CSV) are allowed

Page Name:
ReportingPage

Description:
This page is the official hospital reporting system used for:
- Monthly reports (detailed and numeric)
- Seasonal (quarterly / trimester) HCAT analysis
- Threshold-based evaluation of clinical domains
- Governance-ready exports (PDF / CSV)
- Bulk export preparation across departments

The page supports two orthogonal modes:
1. Report Type:
   - Monthly
   - Seasonal (HCAT-focused)
2. Export Scope:
   - Single report (interactive, filtered)
   - Bulk export (per department summary)

This page is analytical and authoritative.
It consumes data already validated elsewhere (no editing).

UI Components:
- Report type switch (monthly / seasonal)
- Export scope toggle (single / bulk)
- Report filters:
  - Date mode (month / range / trimester)
  - Year, month, trimester
  - Organizational filters (building, idara, dayra, qism)
  - Monthly display mode (detailed vs numeric)
- Threshold input (seasonal reports only)
- Summary alert showing matched complaint count
- Monthly detailed table (row-level complaints)
- Monthly numeric table (aggregated metrics)
- Seasonal detailed HCAT view
- Bulk export table (department counts)
- Report action buttons:
  - Refresh
  - Reset filters
  - Export PDF
  - Export CSV

Your Tasks:

1. Extract **Data Needs** for this page:
   - List all data fields required to:
     - Filter complaints
     - Build monthly detailed tables
     - Build monthly numeric statistics
     - Build seasonal HCAT analysis
     - Evaluate threshold exceedance
     - Populate bulk export summaries
   - Group fields by logical object:
     - Complaint / Incident
     - Patient (minimal identifiers only)
     - Department / Organization hierarchy
     - Classification (HCAT domain, category)
     - Severity / Status
     - Temporal attributes
   - Clearly mark:
     - Derived / aggregated fields
     - Fields reused from other pages (TableView, InvestigationPage)

2. Propose **API Endpoints** for this page:
   - Read-only endpoints for:
     - Fetching filtered complaints
     - Fetching monthly aggregated statistics
     - Fetching seasonal HCAT statistics
     - Fetching grouped HCAT data (domain/category breakdown)
     - Fetching bulk department counts
   - Export endpoints for:
     - Single report PDF export
     - Single report CSV export
     - (Optional) bulk export preparation
   - For each endpoint specify:
     - Endpoint path
     - HTTP method
     - Input parameters (filters, date scope, report type)
     - Output shape (described or simple JSON)
   - Assume pagination for large datasets

3. Identify **assumptions and open questions**, such as:
   - Official definition of HCAT clinical domain
   - How “open” vs “closed” complaints are treated in reports
   - Whether exports must be audit-logged
   - Whether bulk export triggers background jobs
   - Maximum expected data volume for single reports
   - Role-based access to seasonal vs monthly reports

Constraints:
- Do NOT design database tables
- Do NOT write SQL or Python code
- Do NOT add write/update/delete flows
- Keep naming consistent with InvestigationPage and TableView
- Avoid overengineering or speculative analytics

Output Format:
Section A: Data Needs (tables)
Section B: API Endpoints (tables)
Section C: Open Questions / Risks
