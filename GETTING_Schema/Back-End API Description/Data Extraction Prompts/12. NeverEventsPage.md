You are helping me design backend API contracts and data requirements for a React frontend page.

Context:
- This is Iteration 1 only
- The UI is considered finalized
- No future features, no speculation
- Sensitive hospital data, offline server
- This page is READ-ONLY with controlled status transitions
- “Never Events” are zero-tolerance incidents with special governance importance

Page Name:
NeverEventsPage

Description:
This page presents all “Never Events” — critical incidents that must never occur.
The page supports monitoring, trend analysis, and detailed review of each event.

The UI includes:
- KPI summary cards
- Trend chart over time
- Search filter (by record ID, patient name, event type)
- Two tables:
  - Unfinished Never Events
  - Finished Never Events
- A modal for detailed event review

UI Components:
- KPI cards:
  - Total never events
  - Open / unfinished count
  - Finished count
- Trend chart:
  - Never events over time (monthly or seasonal)
- Search input:
  - Free-text search
- Tables:
  - Record identifier
  - Date
  - Patient name
  - Never event type (Arabic + English if available)
  - Severity / category (if applicable)
  - Status (finished / unfinished)
- Details modal:
  - Full never event details
  - Linked incident metadata

Your Tasks:

1. Extract **Data Needs** for this page:
   - List all fields required to:
     - Render KPI cards
     - Build the trend chart
     - Populate both tables
     - Display full details in the modal
   - Group data by logical object:
     - NeverEvent
     - Incident
     - Patient
     - Department / Organization
   - Mark:
     - Derived fields (counts, trends)
     - Status-based groupings (finished vs unfinished)

2. Propose **API Endpoints** for this page:
   - Design read-only endpoints that:
     - Fetch never events with filters
     - Return KPI aggregates
     - Return trend data
     - Fetch detailed information for a single never event
   - For each endpoint specify:
     - Endpoint path
     - HTTP method
     - Input parameters (search, date range, status)
     - Output shape (descriptive JSON)
   - Prefer clean separation between:
     - List endpoints
     - Aggregation endpoints
     - Detail endpoints

3. Identify **assumptions and open questions**, such as:
   - How “Never Event” is defined (flag vs category vs separate entity)
   - Whether a never event is always linked to exactly one incident
   - How status transitions are managed (read-only vs controlled update)
   - Whether historical never events can be reclassified
   - Time granularity for trend charts

Constraints:
- Do NOT design database tables
- Do NOT write SQL or Python code
- Do NOT introduce workflow automation
- Do NOT add write/update APIs unless strictly required
- Keep terminology strict and governance-safe
- Avoid overengineering

Output Format:
Section A: Data Needs (tables)
Section B: API Endpoints (tables)
Section C: Open Questions / Risks
