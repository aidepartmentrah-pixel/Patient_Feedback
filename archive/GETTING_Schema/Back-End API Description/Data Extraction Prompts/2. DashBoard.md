You are helping me design backend API contracts and data requirements for a React frontend page.

Context:
- This is Iteration 1 only
- The UI is considered finalized
- No future features, no speculation
- Focus on READ operations unless clearly editable
- Sensitive hospital data, offline server
- This page is analytics-heavy (aggregations, counts, summaries)

Page Name:
Dashboard

Description:
This page displays a hierarchical dashboard of incident statistics.
The user can switch scope between:
- Hospital (global)
- Administration (Idara)
- Department (Dayra)
- Section (Qism)

Based on the selected scope, the page renders different dashboard statistic components:
- GlobalDashboardStats
- IdaraDashboardStats
- DayraDashboardStats
- QismDashboardStats

The page also provides navigation actions and a link to the Trend Monitoring page.

UI Components:
- Cascading selector (scope + administration + department + section)
- Dashboard title (reflects current scope)
- Statistic cards / summary widgets (counts, distributions)
- Dashboard action buttons (navigation)
- Link card to Trend Monitoring page

Your Tasks:
1. Extract the **Data Needs** for this page:
   - Identify all data required to render dashboard statistics
   - Group fields by logical object (Incident, Department, Severity, Time, etc.)
   - Clearly distinguish between:
     - Raw fields
     - Aggregated / derived metrics (counts, percentages, trends)
   - Consider that different scopes require different aggregation levels

2. Propose **API Endpoints** needed for this page:
   - Endpoint paths
   - HTTP methods (likely GET)
   - Input parameters (scope, entity IDs, date range if applicable)
   - Output shape (high-level description or small JSON examples)
   - Separate endpoints if different scopes require different data

3. Identify **assumptions and open questions**, such as:
   - Default date ranges
   - Whether statistics are computed dynamically or pre-aggregated
   - How scope hierarchy is resolved

Constraints:
- Do NOT design database tables
- Do NOT write SQL or Python code
- Do NOT invent future features
- Keep endpoint naming consistent and clear
- Avoid overengineering

Output Format:
Section A: Data Needs (tables)
Section B: API Endpoints (tables)
Section C: Assumptions / Open Questions / Risks
