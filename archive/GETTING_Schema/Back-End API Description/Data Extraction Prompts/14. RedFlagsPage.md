You are helping me design backend API contracts and data requirements for a React frontend page.

Context:
- This is Iteration 1 only
- The UI is considered finalized
- No future features, no speculation
- Sensitive hospital data
- Offline server environment
- Focus mainly on READ operations
- Minor export action is allowed (PDF generation)

Page Name:
RedFlagsPage

Description:
This page presents a focused view of critical “Red Flag” incidents that require
immediate attention, monitoring, and governance follow-up.

Red Flags represent high-risk incidents (but not necessarily Never Events).
The page provides:
- High-level KPIs
- Trend visualization over time
- Search and filtering
- Separation between unfinished and finished red flags
- Detailed, read-only inspection of a single red flag record
- Optional PDF export for governance documentation

UI Components:
- KPI cards (total red flags, open vs finished, recent counts)
- Trend chart showing red flag occurrences over time
- Summary card linking Red Flags to Never Events
- Search filter (by record ID, patient name)
- Two tables:
  - Unfinished Red Flags
  - Finished Red Flags
- Red Flag details modal:
  - Incident summary
  - Patient information
  - Department and date
  - Red flag classification
  - Status and resolution details
- Export PDF action (single record)

Your Tasks:

1. Extract **Data Needs** for this page:
   - List all required data fields needed to:
     - Populate KPI cards
     - Build the trend chart
     - Populate the Red Flag tables
     - Display the Red Flag details modal
   - Group data by logical object:
     - Red Flag / Incident
     - Patient
     - Department
     - Classification / Severity
     - Status / Resolution
   - Clearly mark:
     - Derived / aggregated fields (counts, trends)
     - Fields shared with Never Events (if any)

2. Propose **API Endpoints** for this page:
   - Read-only endpoints to:
     - Fetch Red Flag KPIs
     - Fetch Red Flag trend data
     - Fetch Red Flag lists (with filters and status)
     - Fetch a single Red Flag details record
   - One optional endpoint for:
     - Exporting a Red Flag report as PDF
   - For each endpoint specify:
     - Endpoint path
     - HTTP method
     - Input parameters (search, status, date range, record ID)
     - Output shape (described or sample JSON)
   - Assume pagination for tables where applicable

3. Identify **assumptions and open questions**, such as:
   - Definition of a Red Flag vs Never Event (classification logic)
   - Whether Red Flags are immutable after being marked “Finished”
   - Who is authorized to export Red Flag PDFs
   - Whether Red Flags must always link to a Follow-Up Action
   - Audit/logging requirements for viewing or exporting critical records

Constraints:
- Do NOT design database tables
- Do NOT write SQL or Python code
- Do NOT add write/update/delete flows (except PDF export)
- Keep naming consistent with NeverEventsPage where possible
- Avoid overengineering or speculative workflows

Output Format:
Section A: Data Needs (tables)
Section B: API Endpoints (tables)
Section C: Open Questions / Risks
