You are helping me design backend API contracts and data requirements for a React frontend page.

Context:
- This is Iteration 1 only
- The UI is considered finalized
- No future features, no speculation
- Sensitive hospital data, offline server
- This page supports BOTH read and update operations
- Focus on tracking and updating follow-up actions

Page Name:
Follow Up Actions

Description:
This page is used to track, manage, and monitor follow-up (corrective) action items
that originate from incident explanations and investigations.

The page provides:
- A calendar-based view of actions
- Time-based grouping (overdue, next 7 days, next 8–14 days)
- Status tracking (pending, delayed, completed)
- Simple update operations (mark complete, delay, update notes)

UI Components:
- Statistics cards:
  - Actions to take
  - Overdue actions
- Filters:
  - Status
  - Priority
  - Department
- Calendar view of actions
- Action summary sections grouped by due date windows
- Action detail modal
- Inline controls:
  - Mark action as completed
  - Delay action (by days or one week)
  - Delete / close action
- Refresh action list

Your Tasks:

1. Extract **Data Needs** for this page:
   - Identify data required for a Follow-Up Action item, including:
     - Action identifier
     - Action title / description
     - Related incident or source reference
     - Department
     - Assigned person
     - Priority
     - Status
     - Due date
     - Completion date
     - Notes / history
   - Identify data required for:
     - Filtering
     - Calendar visualization
     - Time-based grouping (overdue, upcoming)
   - Clearly mark:
     - Derived fields (e.g. overdue flag, days remaining)
     - Display-only fields

2. Propose **API Endpoints** for this page:
   - Endpoints for:
     - Fetching follow-up actions (with filters)
     - Updating an action (status, due date, notes)
     - Marking an action as completed
   - For each endpoint, specify:
     - Endpoint path
     - HTTP method (GET, PUT/PATCH)
     - Input parameters (action_id, update payload)
     - Output shape (updated action or status)
   - Consider partial updates (only changed fields)

3. Identify **assumptions and open questions**, such as:
   - Whether follow-up actions are immutable after completion
   - Whether deletion means soft-close or actual delete
   - How action history / notes are stored
   - How actions are linked to incidents or explanations
   - Who is allowed to update or close actions

Constraints:
- Do NOT design database tables
- Do NOT write SQL or Python code
- Do NOT invent approval workflows
- Keep naming clear and consistent
- Avoid overengineering (simple lifecycle only)

Output Format:
Section A: Data Needs (tables)
Section B: API Endpoints (tables)
Section C: Assumptions / Open Questions / Risks
