
The idea is that I will collect all the data from each one and map it to each table... At the End I will create through this documentation a table view that represents all the variables I need to deal with. I will program the db_layer depending on that. After that I can program the API specified functions as well.... 

I have 2 final goals of this writing here. 
The first is to create a view for me to understand how to operate in terms of programming the API. 
And the second goal of this script is to make  well planned, bit by bit, well coded, verified offline, a transformation procedure. That changes the legacy database to be compatible with the UI Data needs. Including creating new tables and creating new columns in some tables taking into consideration data safety, the working of legacy code, and the aftermath of having to deal with old - limited data within a new data workflow system.

This procedure is split into phases. 
1. **Checking**  the current database to answer some questions
2. **Coding** Write the code to do the action needed
3. **Testing** Test the Code online as a trial
4. **PROCEDURE** Save the Good working Code & Create the PROCEDURE


### THE DATA I NEED TO IMPLEMENT




### Stored Data

**CHECKING**
What are the procedures to be done with stored Data
1. Check the Parameters Table 
2. Check the Encoding

#### Table IncidentRequest

| Unformal Name     | Column                  | Needs Examination and Mapping      |
| ----------------- | ----------------------- | ---------------------------------- |
| Patient Full Name | PatientName             | No                                 |
| Status            | IncidentStatusID        | Yes                                |
|                   | SourceBuilding          | Yes (Parameter)                    |
|                   | DoctorID                | Yes (Needs Explore)                |
|                   | EmployeeID              | Yes (Needs Explore)                |
| In/Out            | IsInPatient             | Yes (bit - mapping)                |
|                   | **DateAndTimeRecieved** | No datetime (NOT NULL)             |
|                   | IncidentRequest         | Explanation To see Forcibly Closed |
|                   | SourceBuilding          |                                    |
|                   | PatientTypeID           |                                    |
|                   | Note                    | (I don't know what is is)          |
|                   |                         |                                    |


#### Table IncidentRequestCase

| Unformal Name | Column                    | Needs Examination and Mapping               |
| ------------- | ------------------------- | ------------------------------------------- |
| Type          | IncidentTypeID            | Yes                                         |
| Category      | IncidentCaseCategoryID    | Yes Parameter                               |
| SubCategory   | IncidentCaseSubCategoryID | I need to check parameter Table             |
| Building      | CaseBuilding              | I need to get data -- Coding                |
|               | DoctorID                  | I need to get data -- Coding                |
|               | Doctor Specialty          | I would make use of it                      |
|               | Description               | I want to see if it is the main description |

#### Table IncidentRequestCaseAction

| Unformal Name | Column                             | Needs Examination and Mapping                                |
| ------------- | ---------------------------------- | ------------------------------------------------------------ |
| Explanation   | DepartmentNote                     | If these are resasonable, we                                 |
| Explanation   | ProblemReason                      | should not create explanation in the create new Column table |
|               |                                    |                                                              |
| The same      | **DateAndTimeHappened**            | UI will not use it                                           |
| The same      | **DateAndTimeSubmit**              | To start the action item duration                            |
|               | **DateAndTimeDepartmentNoteSaved** |                                                              |
|               |                                    |                                                              |


##### Notes:
1. For the UI workflow, **DateAndTimeHappened** and  **DateAndTimeSubmit** are the same. Complaints on my app are two groups. Ordinary complaints that doesn't need action item. And  another group that needs those. But here is my point, the explanation page for the second group is the same one to enter the actions. So both actions and explanations are received in the same date. 

2. Another point to discuss is that `DateAndTimeSubmit` may be null for ordinary complaints. Which is not bad. Coding Note

3. But here I explain an important note, how the time scheduling I demand on the user interface can be made with the 3 dates I have in the table. Received is in the UI and obligatory. Date 2 is the one the departments sends it's explanation. And the third is the `DateAndTimeSubmit` is the one that decides the due date to the action item. Now we can know weather or not the action item is `Overdue` 

4. Another problem to solve is that each action item has many to many relation. I mean that each complaint could have more than one action item. This means the following. The probable solution here to make another table (Like the one about the target department) where each complaint ID (I would rather choose the ID of table 1 to support my idea of one table, or if you find it more logical we can make the IncidentRequestCaseAction Unique ID as well. ) has more than one action item and each action item will have a due date for it. Please Give me a solid opinion here. **(Chat-gpt intervened here and gave me an answer :** ✅ **Correct choice**:  👉 **IncidentRequestCaseID is the correct anchor** )

### Data to Add to Architecture


**Coding**
1. Script to add Columns to Tables. Taking into consideration Default value 
2. Some Scripts to change parameter into independent column
3. Write Code to Create tables
4. Write Code to Mitigate Data from old form to new form (Taking into consideration the precautions)

#### Columns to Add to Tables

| Chosen Table        | Column                                                                                                                      | Type | Default Value | The Value of Old Values                               | Script       |
| ------------------- | --------------------------------------------------------------------------------------------------------------------------- | ---- | ------------- | ----------------------------------------------------- | ------------ |
| IncidentRequestCase | IncidentCaseSeverityID                                                                                                      | int  | Blocked       | Make sure to Make one Encoding                        |              |
| IncidentRequestCase | IncidentCaseDomainID                                                                                                        | int  |               | **LookUpTables**                                      | **script 1** |
| IncidentRequestCase | DepartmentExplanation.                 **We may create this or not. We should first inspect explanation in the part above** | inr  | -             | **LookUpTables** Waiting -Responded - Forcibly Closed | **Script 2** |
| IncidentRequestCase | IncidentCaseEnglishClassificationID                                                                                         | int  |               | **LookUpTables**                                      |              |
| IncidentRequestCase | IncidentCaseStageID                                                                                                         | int  |               | **LookUpTables**                                      |              |
| IncidentRequestCase | IncidentCaseHarmID                                                                                                          | int  |               | **LookUpTables**                                      |              |
| IncidentRequest     | IssuingOrgUnitID                                                                                                            | int  |               | **Primary Key**                                       | **Script 3** |
|                     |                                                                                                                             |      |               |                                                       |              |

When we talk about creating new data we talk about scripts we need to do, 

#### Lookup Tables to Create
For some of the columns to be added in the table above we need as well to add some Lookup table. 
1. **Harm Lookup Table** (No Harm, Minor, Moderate, Sever, Death)
2. **IncidentCaseEnglishClassificationID**
3. **IncidentCaseStageID** 
4. **Domain**

#### Tables to ADD 
When we talk about creating new tables, we talk as well about scripts to do the data migration. We are copying data to a different form. In this case, the New System  can read the data of the old system, but the old system cannot read the updates it has from the new system as they are being stored in different tables of the database. 

1. **IncidentCaseTargetDepartment** Table ( script 4)
This table is needed as each complaint can have more than one target department. 


```
IncidentCaseTargetDepartment
----------------------------
TargetID                 INT            PRIMARY KEY IDENTITY
IncidentRequestCaseID    INT            NOT NULL
DepartmentID             INT            NOT NULL

AssignedAt               DATETIME       NOT NULL
AssignedByUserID         INT            NOT NULL

IsPrimary                BIT            NOT NULL DEFAULT (0)
Frozen                   BIT            NOT NULL DEFAULT (0)
```


2. **IncidentActionItem** (Script 5)

```
ActionItemID              INT            PRIMARY KEY IDENTITY
IncidentRequestCaseID     INT            NOT NULL

ActionTitle               NVARCHAR(200)  NOT NULL
ActionDescription         NVARCHAR(MAX)  NULL

DueDate                   DATETIME       NULL
DateSubmitted             DATETIME       NULL

Done                  BOOL            Done BIT NOT NULL DEFAULT (0)

CreatedAt                 DATETIME       NOT NULL
CreatedByUserID           INT            NOT NULL
```

This is table is created as each complaint will have more than one action item and each on of those sill have it's own due date and Done flag. 
#### Scripts Needed to Mitigate

1. **Domain:** Was stored indirectly in the parameter. And this is not good. I want to make a new independent explicit column named domain. And I need a script to mitigate data from the old form to the new Column. I believe this is safe to the system and would not brake thing out right.  (Let this be called script 1)

2. **Department Explanation:** for this fella, we have in the table IncidentRequestCaseAction something called DepartmentNote and Another thing called problem reason. I should manually inspect the meaning of those. And maybe we don't need to make such a field, and we would only use the DepartmentNote and write to it. Or we can concatenate both texts, as they both fit the tag we call Department Explanation. 

3. **Issuing Department Change:** In the IncidentRequest table, we have 4 info on the issuing.  Of with Chat-gpt we got the brilliant idea of making them 5. But this new one will have a primary key connected to the Administration Table where we have all the departments and sections and stuff. (This replaces the duplicated: SourceSectionID SourceDepartmentID SourceAdminID SourceDepartmentName SourceAdminName)

4. **Creating Target Department table.** This operations involves issues. The first o create the table. And the second is to move the old data to the new table. (Where the UI can actually work and move on)

5. **Creating the IncidentActionItem table.**  This operations as well needs script to move data to create the table in the database this is first. Second we need to copy the action items before with there due date. An important thing to do is to Make all action items done as the UI would explode if like 100 things we not done 10 years before. So this is an safety procedure. 


### Calculated Data

| Table     | Field                        | Type    | Description                          | Table               | Column                     |
| --------- | ---------------------------- | ------- | ------------------------------------ | ------------------- | -------------------------- |
| Dashboard | `totalIncidents`             | Integer | Count of all incidents               | IncidentRequest     | Cardinality of Table       |
| Dashboard | `uniquePatients`             | Integer | Count of distinct patients           | IncidentRequest     | Pateintname                |
| Dashboard | `openClosed.open`            | Integer | Count of open incidents              | IncidentRequest     | Open/Closed/ForciblyClosed |
| Dashboard | `openClosed.closed`          | Integer | Count of closed incidents            | IncidentRequest     | Open/Closed/ForciblyClosed |
| Dashboard | `openClosed.forciblyClosed`  | Integer | Count of closed incidents            | IncidentRequest     | Open/Closed/ForciblyClosed |
| Dashboard | `severityBreakdown.high`     | Integer | Count of high severity incidents     | IncidentRequestCase | IncidentCaseSeverityID     |
| Dashboard | `severityBreakdown.medium`   | Integer | Count of medium severity incidents   | IncidentRequestCase | IncidentCaseSeverityID     |
| Dashboard | `severityBreakdown.low`      | Integer | Count of low severity incidents      | IncidentRequestCase | IncidentCaseSeverityID     |
| Dashboard | `domainBreakdown.clinical`   | Integer | Count of clinical domain incidents   | IncidentRequestCase | IncidentCaseDomainID       |
| Dashboard | `domainBreakdown.management` | Integer | Count of management domain incidents | IncidentRequestCase | IncidentCaseDomainID       |
| Dashboard | `domainBreakdown.relational` | Integer | Count of relational domain incidents | IncidentRequestCase | IncidentCaseDomainID       |
|           |                              |         |                                      |                     |                            |


### Cached Data


| Table     | Field            | Type    | Description            | Table           | Column |
| --------- | ---------------- | ------- | ---------------------- | --------------- | ------ |
| Dashbaord | `totalIncidents` | Integer | Count of all incidents | IncidentRequest |        |

### Transient (UI / Workflow / Request) Data















### Some Documentations

1. **DepartmentExplanation**  represents the current governance state of a case with respect to departmental response, distinguishing between pending response, completed response, and supervisor-forced closure; it is modeled as a controlled lookup and stored at the case level to support clear UI behavior, analytics, and workflow enforcement



### Some Findings

1. This schema is connected to the another server. This database intentionally stores only external identifiers for doctors and patients, relying on upstream hospital systems as the authoritative source of truth, which is a correct and common architectural choice in healthcare environments. If this means anything, it means that the database of the patients is not totally independent from other things. This means the information I was given was wrong '**'The patient system database is independent"**



### Some question Still in the Back of my Head for Months!!! 
It is really a shame to hold such a idea for too long without understanding it. So let's have the guts to ask at least. 
1. **The Architecture Question Here.** After this long journey of choosing out which bits of the database are needed by the Website UI I am building. I can now understand that I have a database of many tables. But at the same time I can understand and visualize on table that presents all this data at once (Me being a beginner I need this emotional feeling). The historical question arises. I now have some backend endpoint functions (I don't understand those as well, they are written by Chat-gpt and are templates to be Updated by Copilot not me) Talking into consideration such API. What function do I need to program to implement these API know. Can we say that the progress and context of the project is mature enough to answer this question and finally let me understand it for once as a decent human being. 


```
"""  
AUTO-GENERATED API CONTRACT  
  
Source: Obsidian – Dashboard Page  
Iteration: 1  
Status: API skeleton only – no implementation  
"""  
  
from datetime import date, datetime  
from enum import Enum  
from typing import Dict, List, Optional  
  
from fastapi import APIRouter, Query  
from pydantic import BaseModel  
  
  
router = APIRouter(prefix="/api/dashboard", tags=["dashboard"])  
  
  
# -----------------------------  
# Enums  
# -----------------------------  
  
class TrendDirection(str, Enum):  
    up = "up"  
    down = "down"  
  
  
class SeverityLevel(str, Enum):  
    High = "High"  
    Medium = "Medium"  
    Low = "Low"  
  
  
class IncidentStatus(str, Enum):  
    Open = "Open"  
    Pending = "Pending"  
    Closed = "Closed"  
  
  
# -----------------------------  
# Core Metric Models  
# -----------------------------  
  
class OpenClosedMetrics(BaseModel):  
    open: int  
    closed: int  
    forciblyClosed: int  
  
  
class SeverityBreakdown(BaseModel):  
    high: int  
    medium: int  
    low: int  
  
  
class DomainBreakdown(BaseModel):  
    clinical: int  
    management: int  
    relational: int  
  
  
class DashboardMetrics(BaseModel):  
    totalIncidents: int  
    uniquePatients: int  
    openClosed: OpenClosedMetrics  
    severityBreakdown: SeverityBreakdown  
    domainBreakdown: DomainBreakdown  
    redFlags: int  
  
  
# -----------------------------  
# Trend Models  
# -----------------------------  
  
class TrendMetric(BaseModel):  
    value: int  
    direction: TrendDirection  
  
  
class DashboardTrends(BaseModel):  
    incidentsPatients: TrendMetric  
    openClosed: TrendMetric  
    severity: TrendMetric  
    domain: TrendMetric  
    redFlags: TrendMetric  
  
  
# -----------------------------  
# Chart Models  
# -----------------------------  
  
class TopClassificationItem(BaseModel):  
    classification: str  
    count: int  
  
  
class StageHistogramItem(BaseModel):  
    stage: str  
    count: int  
  
  
class IssuingDepartmentItem(BaseModel):  
    department: str  
    count: int  
  
  
class DashboardCharts(BaseModel):  
    top5Classification: List[TopClassificationItem]  
    stageHistogram: List[StageHistogramItem]  
    issuingDept: Optional[List[IssuingDepartmentItem]] = None  
  
  
# -----------------------------  
# Recent Activity Models  
# -----------------------------  
  
class RecentActivityItem(BaseModel):  
    timestamp: datetime  
    description: str  
    severity: SeverityLevel  
    status: IncidentStatus  
  
  
# -----------------------------  
# Dashboard Statistics Response  
# -----------------------------  
  
class DashboardStatsResponse(BaseModel):  
    metrics: DashboardMetrics  
    trends: DashboardTrends  
    charts: DashboardCharts  
    recentActivity: List[RecentActivityItem]  
  
  
# -----------------------------  
# Organizational Hierarchy Models  
# -----------------------------  
  
class OrganizationUnit(BaseModel):  
    id: str  
    nameAr: str  
    nameEn: str  
  
  
class DashboardHierarchyResponse(BaseModel):  
    idarat: List[OrganizationUnit]  
    dayrat: Dict[str, List[OrganizationUnit]]  
    aqsam: Dict[str, List[OrganizationUnit]]  
  
  
# -----------------------------  
# API Endpoints  
# -----------------------------  
  
@router.get(  
    "/stats",  
    response_model=DashboardStatsResponse  
)  
def get_dashboard_stats(  
    scope: str = Query(..., description="Scope level: hospital, administration, department, section"),  
    administration_id: Optional[str] = Query(None),  
    department_id: Optional[str] = Query(None),  
    section_id: Optional[str] = Query(None),  
    start_date: Optional[date] = Query(None),  
    end_date: Optional[date] = Query(None),  
):  
    raise NotImplementedError  
  
  
@router.get(  
    "/hierarchy",  
    response_model=DashboardHierarchyResponse  
)  
def get_dashboard_hierarchy():  
    raise NotImplementedError
```




### The Architecture Talk (After Launch)